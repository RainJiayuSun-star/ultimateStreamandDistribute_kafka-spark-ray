1.
1. For the ALL STATION DATA section, change it to similar style as the kafka topics section. I want each station be a big card, and inside there 3 sections: Raw data, features, prediction. And inside the
2. can you also sync each part of the docker compose logs to a expandable window